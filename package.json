{
  "name": "idlm-ai-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "idlm's personal AI workbench — static front end (Vercel) + OpenAI proxy (api/chat.js)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "deploy-preview": "vercel deploy"
  },
  "dependencies": {
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "vercel": "^59.9.1"
  }
}
